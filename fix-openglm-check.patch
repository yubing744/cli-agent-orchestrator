--- a/src/cli_agent_orchestrator/providers/open_autoglm.py
+++ b/src/cli_agent_orchestrator/providers/open_autoglm.py
@@ -70,7 +70,7 @@

     def initialize(self) -> bool:
         """Initialize OpenAutoGLM provider by starting the CLI."""
         try:
-            cmd = self._build_openautoglm_command()
+            cmd = self._build_openautoglm_command() + " --skip-system-check"
             session_id = tmux_client.new_session(
                 self.tmux_session, window_name=self.tmux_window
             )
             tmux_client.send_keys(self.tmux_session, self.tmux_window, cmd)

@@ -88,7 +88,7 @@
         except Exception as e:
-            raise ProviderError(f"Failed to initialize OpenAutoGLM provider: {e}")
+            raise ProviderError(f"Failed to initialize OpenAutoGLM provider: {e}")

     def _build_openautoglm_command(self) -> str:
         """Build OpenAutoGLM command with environment configuration."""
@@ -109,6 +109,15 @@

         # Build command with uvx for proper dependency management
         command = f"""{env_setup}
-cd {openautoglm_dir} && uvx --python 3.11 --with openai --with pillow python main.py"""

         # For CAO integration, we'll use interactive mode
         return command
+
+    def _check_device_specific_requirements(self) -> bool:
+        """Check device-specific requirements (ADB keyboard, etc.) for the specified device."""
+        try:
+            # Check if device is connected
+            device_check = subprocess.run(
+                ["adb", "-s", "127.0.0.1:5555", "shell", "echo", "device_check"],
+                capture_output=True, text=True,
+                timeout=10,
+            )
+            if device_check.returncode != 0:
+                return False
+
+            # Check ADB keyboard on the specific device
+            keyboard_check = subprocess.run(
+                ["adb", "-s", "127.0.0.1:5555", "shell", "ime", "list", "-s"],
+                capture_output=True,
+                text=True,
+                timeout=10,
+            )
+            return "com.android.adbkeyboard/.AdbIME" in keyboard_check.stdout
+        except Exception as e:
+            return False
+
     def handle_termination(self, reason: str) -> bool:
         """Handle termination of terminal session."""
         return True
--- a/src/cli_agent_orchestrator/providers/open_autoglm.py
+++ b/src/cli_agent_orchestrator/providers/openautoglm.py
@@ -1,4 +1,12 @@
+                return True
+
+    def start_without_system_check(self) -> bool:
+        """Start OpenAutoGLM provider without running system checks."""
+        try:
+            # Skip system checks and directly start
+            cmd = self._build_openautoglm_command() + " --skip-system-check"
+            session_id = tmux_client.new_session(
+                self.tmux_session, window_name=self.tmux_window
+            )
+            tmux_client.send_keys(self.tmux_session, self.tmux_window, cmd)
+
+            print("✅ OpenAutoGLM启动成功 (跳过系统检查)")
+            return True
+        except Exception as e:
+            raise ProviderError(f"Failed to start OpenAutoGLM: {e}")
+
     def handle_termination(self, reason: str) -> bool:
         return True